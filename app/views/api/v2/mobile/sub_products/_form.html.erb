<%= form_with(model: api_v2_mobile_sub_product, local: true) do |form| %>
  <% if api_v2_mobile_sub_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_v2_mobile_sub_product.errors.count, "error") %> prohibited this api_v2_mobile_sub_product from being saved:</h2>

      <ul>
      <% api_v2_mobile_sub_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
